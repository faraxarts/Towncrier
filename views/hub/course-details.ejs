<%
  /*
   * Accepts different image formats that may have been saved in the database:
   * - "/uploads/image.jpg"
   * - "uploads/image.jpg"
   * - "public/images/image.jpg"
   * - { fileUrl: "..." }
   * - { url: "..." }
   * - { path: "..." }
   */
  const resolveMediaUrl = (media, fallback) => {
    let mediaUrl = media;

    if (mediaUrl && typeof mediaUrl === "object") {
      mediaUrl =
        mediaUrl.fileUrl ||
        mediaUrl.secureUrl ||
        mediaUrl.secure_url ||
        mediaUrl.url ||
        mediaUrl.path ||
        (mediaUrl.filename
          ? `/uploads/${mediaUrl.filename}`
          : "");
    }

    if (!mediaUrl || typeof mediaUrl !== "string") {
      return fallback;
    }

    mediaUrl = mediaUrl.trim().replace(/\\/g, "/");

    if (
      /^(https?:)?\/\//i.test(mediaUrl) ||
      mediaUrl.startsWith("data:") ||
      mediaUrl.startsWith("blob:")
    ) {
      return mediaUrl;
    }

    if (mediaUrl.startsWith("public/")) {
      return `/${mediaUrl.slice("public/".length)}`;
    }

    return mediaUrl.startsWith("/")
      ? mediaUrl
      : `/${mediaUrl}`;
  };

  const courseImageUrl = resolveMediaUrl(
    course.image || course.featuredImage || course.imageUrl,
    "/images/event1.jpg"
  );

  const pageDescription =
    typeof metaDescription !== "undefined" && metaDescription
      ? metaDescription
      : course.description ||
        `View course information for ${course.title}.`;
%>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />

  <meta
    name="viewport"
    content="width=device-width, initial-scale=1.0"
  />

  <title><%= course.title %> | <%= academy.name %></title>

  <meta
    name="description"
    content="<%= pageDescription %>"
  />

  <% if (typeof canonicalUrl !== "undefined" && canonicalUrl) { %>
    <link rel="canonical" href="<%= canonicalUrl %>" />
  <% } %>

  <% if (typeof noindex !== "undefined" && noindex) { %>
    <meta name="robots" content="noindex" />
  <% } %>

  <link
    rel="icon"
    type="image/png"
    href="/images/tcem-logo.png"
  />

  <link
    rel="apple-touch-icon"
    href="/images/tcem-logo.png"
  />

  <link
    rel="preconnect"
    href="https://fonts.googleapis.com"
  />

  <link
    rel="preconnect"
    href="https://fonts.gstatic.com"
    crossorigin
  />

  <link
    href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap"
    rel="stylesheet"
  />

  <link rel="stylesheet" href="/css/style.css" />

  <style>
    /*
     * Prevent long document, audio and assignment names
     * from overflowing their cards.
     */
    .course-file-row {
      min-width: 0;
    }

    .course-file-information {
      flex: 1;
      min-width: 0;
    }

    .course-file-name {
      display: block;
      max-width: 100%;
      overflow-wrap: anywhere;
      word-break: break-word;
      line-height: 1.45;
    }

    .course-audio-card {
      min-width: 0;
      overflow: hidden;
    }

    .course-audio-header {
      display: flex;
      align-items: flex-start;
      gap: 0.75rem;
      min-width: 0;
      margin-bottom: 0.85rem;
    }

    .course-audio-information {
      flex: 1;
      min-width: 0;
    }

    .course-audio-player {
      display: block;
      width: 100%;
      max-width: 100%;
      min-width: 0;
    }

    /*
     * Makes the image area retain its space while loading.
     */
    .course-featured-image {
      display: block;
      width: 100%;
      height: 280px;
      object-fit: cover;
      background: #f1f5f9;
    }

    @media (min-width: 768px) {
      .course-featured-image {
        height: 420px;
      }
    }
  </style>
</head>

<body class="bg-[#f7f7f8] text-gray-900">

  <%- include("../partials/header") %>

  <!-- COURSE HERO -->
  <section
    class="bg-gradient-to-r from-[#4F46E5] via-[#7C3AED] to-[#C026D3] text-white"
  >
    <div class="max-w-7xl mx-auto px-6 md:px-16 py-10 md:py-12">
      <div
        class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-6"
      >
        <div>
          <p class="text-white/80 text-sm md:text-base mb-3">
            <a
              href="/academy"
              class="hover:text-white transition"
            >
              Academy
            </a>

            <span class="mx-2">/</span>

            <a
              href="/academy/<%= academy.slug %>"
              class="hover:text-white transition"
            >
              <%= academy.name %>
            </a>

            <span class="mx-2">/</span>

            <a
              href="/academy/<%= academy.slug %>/courses"
              class="hover:text-white transition"
            >
              Courses
            </a>

            <span class="mx-2">/</span>

            <span class="text-white font-semibold">
              <%= course.title %>
            </span>
          </p>

          <h1 class="text-3xl md:text-5xl font-extrabold mb-3">
            <%= course.title %>
          </h1>

          <p class="text-lg md:text-2xl text-white/90 max-w-4xl">
            <%= course.description %>
          </p>
        </div>

        <% if (isLoggedIn) { %>
          <a
            href="/academy/<%= academy.slug %>/documents"
            class="inline-flex items-center justify-center rounded-2xl bg-white text-[#4F46E5] px-8 py-4 text-lg font-semibold shadow-sm hover:shadow-md transition self-start lg:self-auto"
          >
            My Documents
          </a>
        <% } else { %>
          <a
            href="/academy/<%= academy.slug %>"
            class="inline-flex items-center justify-center rounded-2xl bg-white text-[#4F46E5] px-8 py-4 text-lg font-semibold shadow-sm hover:shadow-md transition self-start lg:self-auto"
          >
            Back to Academy
          </a>
        <% } %>
      </div>
    </div>

    <!-- ACADEMY NAVIGATION -->
    <div class="border-t border-white/15">
      <div class="max-w-7xl mx-auto px-6 md:px-16">
        <div class="flex items-center gap-8 overflow-x-auto">
          <a
            href="/academy/<%= academy.slug %>/courses"
            class="inline-flex h-14 items-center border-b-2 border-white text-white font-semibold whitespace-nowrap"
          >
            <span class="mr-2" aria-hidden="true">📖</span>
            Courses
          </a>

          <a
            href="/academy/<%= academy.slug %>/documents"
            class="inline-flex h-14 items-center border-b-2 border-transparent text-white/70 font-medium whitespace-nowrap hover:text-white transition"
          >
            <span class="mr-2" aria-hidden="true">📄</span>
            Documents
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- COURSE CONTENT -->
  <section class="py-12 md:py-14">
    <div class="max-w-7xl mx-auto px-6 md:px-16">
      <div class="grid grid-cols-1 xl:grid-cols-3 gap-8">

        <!-- MAIN COLUMN -->
        <div class="xl:col-span-2 space-y-8">

          <!-- COURSE IMAGE -->
          <div
            class="bg-white border border-gray-100 rounded-[28px] shadow-sm overflow-hidden"
          >
            <img
              src="<%= courseImageUrl %>"
              alt="<%= course.title %>"
              width="1200"
              height="675"
              loading="eager"
              fetchpriority="high"
              decoding="async"
              class="course-featured-image"
              onerror="this.onerror=null; this.src='/images/event1.jpg';"
            />
          </div>

          <!-- COURSE OVERVIEW -->
          <div
            class="bg-white border border-gray-100 rounded-[28px] shadow-sm p-8 md:p-10"
          >
            <h2
              class="text-3xl md:text-4xl font-extrabold text-gray-950 mb-5"
            >
              Course Overview
            </h2>

            <p
              class="text-lg md:text-[1.15rem] leading-relaxed text-slate-700 whitespace-pre-line"
            >
              <%= course.overview || course.description %>
            </p>
          </div>

          <!-- WHAT YOU WILL LEARN -->
          <div
            class="bg-white border border-gray-100 rounded-[28px] shadow-sm p-8 md:p-10"
          >
            <h2
              class="text-3xl md:text-4xl font-extrabold text-gray-950 mb-6"
            >
              What You Will Learn
            </h2>

            <% if (
              course.whatYouWillLearn &&
              course.whatYouWillLearn.length
            ) { %>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <% course.whatYouWillLearn.forEach((item) => { %>
                  <div
                    class="flex items-start gap-3 rounded-2xl bg-indigo-50/70 px-5 py-4"
                  >
                    <span
                      class="text-[#4F46E5] text-xl mt-0.5"
                      aria-hidden="true"
                    >
                      ✓
                    </span>

                    <p class="text-base md:text-lg text-slate-700">
                      <%= item %>
                    </p>
                  </div>
                <% }) %>
              </div>
            <% } else { %>
              <p class="text-lg text-slate-600">
                No learning outcomes added yet.
              </p>
            <% } %>
          </div>

        </div>

        <!-- SIDEBAR -->
        <aside class="space-y-8">

          <!-- COURSE DETAILS -->
          <div
            class="bg-white border border-gray-100 rounded-[28px] shadow-sm p-8"
          >
            <h2
              class="text-2xl md:text-3xl font-extrabold text-gray-950 mb-6"
            >
              Course Details
            </h2>

            <div class="space-y-4 text-base md:text-lg text-slate-700">

              <div class="flex items-start justify-between gap-4">
                <span class="font-semibold shrink-0">Duration</span>
                <span class="text-right break-words min-w-0">
                  <%= course.duration || "Not specified" %>
                </span>
              </div>

              <div class="flex items-start justify-between gap-4">
                <span class="font-semibold shrink-0">Lessons</span>
                <span class="text-right break-words min-w-0">
                  <%= course.lessons || "Not specified" %>
                </span>
              </div>

              <div class="flex items-start justify-between gap-4">
                <span class="font-semibold shrink-0">Instructor</span>
                <span class="text-right break-words min-w-0">
                  <%= course.instructor || "Not assigned" %>
                </span>
              </div>


              <div class="flex items-start justify-between gap-4">
                <span class="font-semibold shrink-0">Students</span>
                <span class="text-right">
                  <%= course.students || 0 %>
                </span>
              </div>

              <div class="flex items-start justify-between gap-4">
                <span class="font-semibold shrink-0">Rating</span>
                <span class="text-right">
                  <%= course.rating || 0 %>
                </span>
              </div>

            </div>
          </div>

          <!-- COURSE DOCUMENTS -->
          <div
            class="bg-white border border-gray-100 rounded-[28px] shadow-sm p-8"
          >
            <h2
              class="text-2xl md:text-3xl font-extrabold text-gray-950 mb-6"
            >
              Course Documents
            </h2>

            <% if (
              course.courseDocuments &&
              course.courseDocuments.length
            ) { %>
              <div class="space-y-4">
                <% course.courseDocuments.forEach((doc, index) => { %>
                  <a
                    href="<%= doc.fileUrl %>"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="course-file-row flex items-center justify-between gap-4 rounded-2xl border border-gray-100 bg-gray-50/80 px-5 py-4 hover:border-[#6B46C1] hover:bg-purple-50/40 transition"
                  >
                    <div
                      class="course-file-information flex items-start gap-3"
                    >
                      <span
                        class="text-2xl shrink-0"
                        aria-hidden="true"
                      >
                        📄
                      </span>

                      <div class="course-file-information">
                        <p class="font-semibold text-gray-900">
                          Document <%= index + 1 %>
                        </p>

                        <p
                          class="course-file-name text-sm text-slate-600"
                          title="<%= doc.originalName || 'Course document' %>"
                        >
                          <%= doc.originalName || "Course document" %>
                        </p>
                      </div>
                    </div>

                    <span
                      class="text-[#6B46C1] font-semibold shrink-0"
                    >
                      Open
                    </span>
                  </a>
                <% }) %>
              </div>
            <% } else { %>
              <p class="text-lg text-slate-600">
                No course documents uploaded yet.
              </p>
            <% } %>
          </div>

          <!-- COURSE AUDIOS -->
          <div
            class="bg-white border border-gray-100 rounded-[28px] shadow-sm p-8"
          >
            <h2
              class="text-2xl md:text-3xl font-extrabold text-gray-950 mb-6"
            >
              Course Audios
            </h2>

            <% if (
              course.courseAudios &&
              course.courseAudios.length
            ) { %>
              <div class="space-y-5">
                <% course.courseAudios.forEach((audio, index) => { %>
                  <div
                    class="course-audio-card rounded-2xl border border-gray-100 bg-gray-50/80 px-5 py-4"
                  >
                    <div class="course-audio-header">
                      <span
                        class="text-2xl shrink-0"
                        aria-hidden="true"
                      >
                        🎧
                      </span>

                      <div class="course-audio-information">
                        <p class="font-semibold text-gray-900">
                          Audio <%= index + 1 %>
                        </p>

                        <p
                          class="course-file-name text-sm text-slate-600"
                          title="<%= audio.originalName || 'Course audio' %>"
                        >
                          <%= audio.originalName || "Course audio" %>
                        </p>
                      </div>
                    </div>

                    <audio
                      controls
                      preload="metadata"
                      class="course-audio-player"
                    >
                      <source
                        src="<%= audio.fileUrl %>"
                        type="<%= audio.mimeType || 'audio/mpeg' %>"
                      />

                      Your browser does not support the audio player.
                    </audio>
                  </div>
                <% }) %>
              </div>
            <% } else { %>
              <p class="text-lg text-slate-600">
                No course audios uploaded yet.
              </p>
            <% } %>
          </div>

          <!-- ASSIGNMENT -->
          <div
            class="bg-white border border-gray-100 rounded-[28px] shadow-sm p-8"
          >
            <h2
              class="text-2xl md:text-3xl font-extrabold text-gray-950 mb-6"
            >
              Assignment
            </h2>

            <% if (
              course.assignmentDocument &&
              course.assignmentDocument.fileUrl
            ) { %>
              <a
                href="<%= course.assignmentDocument.fileUrl %>"
                target="_blank"
                rel="noopener noreferrer"
                class="course-file-row flex items-center justify-between gap-4 rounded-2xl border border-gray-100 bg-gray-50/80 px-5 py-4 hover:border-[#6B46C1] hover:bg-purple-50/40 transition"
              >
                <div
                  class="course-file-information flex items-start gap-3"
                >
                  <span
                    class="text-2xl shrink-0"
                    aria-hidden="true"
                  >
                    📝
                  </span>

                  <div class="course-file-information">
                    <p class="font-semibold text-gray-900">
                      Assignment Document
                    </p>

                    <p
                      class="course-file-name text-sm text-slate-600"
                      title="<%= course.assignmentDocument.originalName || 'Assignment document' %>"
                    >
                      <%= course.assignmentDocument.originalName || "Assignment document" %>
                    </p>
                  </div>
                </div>

                <span
                  class="text-[#6B46C1] font-semibold shrink-0"
                >
                  Open
                </span>
              </a>
            <% } else { %>
              <p class="text-lg text-slate-600">
                No assignment document uploaded yet.
              </p>
            <% } %>
          </div>

        </aside>
      </div>
    </div>
  </section>

  <%- include("../partials/footer") %>

</body>
</html>