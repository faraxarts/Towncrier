<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Event | Admin</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body class="bg-[#f7f7f8] text-gray-900">

  <div class="min-h-screen lg:grid lg:grid-cols-[280px_1fr]">
    <%- include("../partials/admin-sidebar") %>

    <main class="px-6 py-8 md:px-10 lg:px-12">

  <section class="py-10 px-6 md:px-12">
    <div class="max-w-5xl mx-auto">
      <div class="flex items-center justify-between mb-8">
        <div>
          <h1 class="text-4xl font-extrabold text-gray-950 mb-2">Edit Event</h1>
          <p class="text-lg text-slate-600"><%= event.title %></p>
        </div>

        <a href="/admin/events" class="rounded-2xl bg-white border border-gray-200 px-5 py-3 font-semibold text-gray-800 hover:border-[#6B46C1] hover:text-[#6B46C1] transition">
          Back
        </a>
      </div>

      <form action="/admin/events/<%= event._id %>/update" method="POST" enctype="multipart/form-data" class="bg-white border border-gray-100 rounded-[28px] shadow-sm p-8 md:p-10 space-y-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <label class="block text-sm font-semibold mb-2">Event Title</label>
            <input name="title" type="text" value="<%= event.title %>" class="w-full rounded-2xl border border-gray-200 px-4 py-3.5" required>
          </div>

          <div>
            <label class="block text-sm font-semibold mb-2">Slug</label>
            <input name="slug" type="text" value="<%= event.slug %>" class="w-full rounded-2xl border border-gray-200 px-4 py-3.5">
          </div>
        </div>

        <div>
          <label class="block text-sm font-semibold mb-2">Replace Featured Image</label>
          <input name="featuredImage" type="file" accept=".jpg,.jpeg,.png,.webp" class="w-full rounded-2xl border border-gray-200 px-4 py-3.5">
          <% if (event.featuredImage) { %>
            <p class="text-sm text-slate-500 mt-2">Current: <%= event.featuredImage %></p>
          <% } %>
        </div>

        <div>
          <label class="block text-sm font-semibold mb-2">Short Description</label>
          <textarea name="shortDescription" rows="4" class="w-full rounded-2xl border border-gray-200 px-4 py-3.5" required><%= event.shortDescription %></textarea>
        </div>

        <div>
          <label class="block text-sm font-semibold mb-2">Full Description</label>
          <textarea name="fullDescription" rows="5" class="w-full rounded-2xl border border-gray-200 px-4 py-3.5"><%= event.fullDescription || "" %></textarea>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <div>
            <label class="block text-sm font-semibold mb-2">Event Date</label>
            <input name="eventDate" type="text" value="<%= event.eventDate %>" class="w-full rounded-2xl border border-gray-200 px-4 py-3.5">
          </div>

          <div>
            <label class="block text-sm font-semibold mb-2">Event Time</label>
            <input name="eventTime" type="text" value="<%= event.eventTime %>" class="w-full rounded-2xl border border-gray-200 px-4 py-3.5">
          </div>

          <div>
            <label class="block text-sm font-semibold mb-2">Category</label>
            <input name="category" type="text" value="<%= event.category %>" class="w-full rounded-2xl border border-gray-200 px-4 py-3.5">
          </div>
        </div>

        <div>
          <label class="block text-sm font-semibold mb-2">Location</label>
          <input name="location" type="text" value="<%= event.location %>" class="w-full rounded-2xl border border-gray-200 px-4 py-3.5">
        </div>

        <div>
          <label class="block text-sm font-semibold mb-2">Button Text</label>
          <input name="buttonText" type="text" value="<%= event.buttonText %>" class="w-full rounded-2xl border border-gray-200 px-4 py-3.5">
        </div>

        <div class="flex flex-wrap gap-6">
          <label class="inline-flex items-center gap-3">
            <input type="checkbox" name="isPublished" <%= event.isPublished ? "checked" : "" %>>
            <span>Published</span>
          </label>
        </div>

        <button type="submit" class="rounded-2xl bg-[#6B46C1] text-white px-8 py-4 font-semibold hover:bg-purple-700 transition">
          Update Event
        </button>
      </form>
    </div>
  </section>

    </main>
  </div>

  <script src="/js/admin-shell.js"></script>
</body>
</html>